{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window: read attention.dump via Rust command only",
  "windows": ["main"],
  "permissions": ["core:default"]
}
