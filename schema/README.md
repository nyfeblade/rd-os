# schema/ — types

TypeScript shapes for machine-time, experiment, MCP reject codes, and attention dump.

Runtime enforcement is `src/kernel.js` + `bin/rdos.js` (file store, not an MCP server).

See `ARCHITECTURE.md` and `MCP_CONTRACT.md` for HARD LAW.
